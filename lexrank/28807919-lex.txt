lexrank.summarize("if the array be define in the file but be empty, like: then CW will return a empty array, but the object be not null. then, if you do this: CW be not null and CW will throw a exception - there be no first element in the array. instead you can test the CW, e.g.: you can also use cw method, this be the method we use to check whether the list be empty or not. this method return a boolean value. it return true if the list be empty otherwise it give false. for example:", 1, function (err, toplines, text) { console.log(toplines); });

[ { weight: 0.5467507916112146,
    text: 'if the array be define in the file but be empty, like: then CW will return a empty array, but the object be not null.',
    index: 0 } ]