lexrank.summarize("you have to fetch the whole datum in the list and then do the iteration as it be a file and will become inefficient otherwise. just parse as a array: then if you need you can also create a list in this way: P.S. you json string should be look like this: in case you need to parse it from a file, I find the best solution to use a cw to use it inside you java code for better manipultion. try out this code:", 1, function (err, toplines, text) { console.log(toplines); });

[ { weight: 0.5794702609988028,
    text: 'you json string should be look like this: in case you need to parse it from a file, I find the best solution to use a cw to use it inside you java code for better manipultion.',
    index: 2 } ]